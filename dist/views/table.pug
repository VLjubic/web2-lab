html
    head
        title web2 lab
    body
        h1 Welcome #{name} to the table page
        ul
            li: a(href='/games', title='games') Utakmice
            li: a(href='/', title='Home') Home

        table(style='width:100%', border='1')
                tr
                    th Br.
                    th Ime ekipe
                    th Broj bodova
                    th Gol razlika
                each val, index in table
                    tr(class="row")
                        td #{index+1}
                        each val2, key in val
                            td #{val2}
